name: linux-arm64

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  linux-arm64:
    uses: ./.github/workflows/_build-unix.yml
    with:
      os: ubuntu-24.04-arm
