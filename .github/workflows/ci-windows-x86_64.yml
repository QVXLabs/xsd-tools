name: windows-x86_64

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  windows-x86_64:
    uses: ./.github/workflows/_build-windows.yml
    with:
      os: windows-latest
