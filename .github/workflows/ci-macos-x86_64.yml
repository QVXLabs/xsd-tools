name: macos-x86_64

# Intel macOS: no free hosted runner remains; macos-15-large is billed.
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    uses: ./.github/workflows/_build-unix.yml
    with:
      os: macos-15-large
