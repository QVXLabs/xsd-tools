name: linux-x86_64

on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  linux-x86_64:
    uses: ./.github/workflows/_build-unix.yml
    with:
      os: ubuntu-latest
