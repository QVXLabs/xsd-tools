*.o
*.luac
*.pyc
xsdb
# CMake / Conan build tree
/build/
# Packaging build tree + output artifacts (pkg/build-packages.sh)
/build-pkg/
/dist/
conanbuildinfo.*
conaninfo.txt
graph_info.json
activate*.sh
deactivate*.sh
environment*.env

# Claude planning docs — local only, not part of the repo
/revitalize*.md
